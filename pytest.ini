[pytest]
# TST-002：根目录 pytest 配置，将测试发现重定向到 backend/tests
# 使开发者可在项目根目录直接运行 pytest，无需先 cd backend
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning:starlette.*
